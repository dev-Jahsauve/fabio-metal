import {notFound} from 'next/navigation'; import Link from 'next/link'; import {getCurrentUser} from '@/lib/auth'; import {db} from '@/lib/db'; import {and,eq} from 'drizzle-orm'; import {orders,orderItems,payments,shipments,invoices} from '@/lib/db/schema'; import {formatXaf,orderReference} from '@/lib/utils';
export default async function Commande({params}:{params:Promise<{id:string}>}){const u=await getCurrentUser();if(!u)return <main className="section"><div className="container"><div className="card"><h1>Connexion requise</h1><Link className="btn btn-gold" href="/connexion">Connexion</Link></div></div></main>;const {id}=await params;const [o]=await db.select().from(orders).where(u.role==='admin'?eq(orders.id,id):and(eq(orders.id,id),eq(orders.userId,u.id))).limit(1);if(!o)return notFound();const items=await db.select().from(orderItems).where(eq(orderItems.orderId,o.id));const [p]=await db.select().from(payments).where(eq(payments.orderId,o.id)).limit(1);const [s]=await db.select().from(shipments).where(eq(shipments.orderId,o.id)).limit(1);const [i]=await db.select().from(invoices).where(eq(invoices.orderId,o.id)).limit(1);return <main className="section"><div className="container"><span className="eyebrow">Commande</span><h1>{orderReference(o.id)}</h1><div className="cards"><div className="card"><h3>Statut</h3><p>{o.status}</p><p>Paiement : {p?.status||'—'}</p><p>Livraison : {s?.status||'—'}</p></div><div className="card"><h3>Total</h3><div className="price">{formatXaf(o.totalXaf)}</div><p>Sous-total : {formatXaf(o.subtotalXaf)}<br/>Livraison : {formatXaf(o.deliveryFeeXaf)}</p></div></div><div className="card" style={{marginTop:18}}><h2>Articles</h2>{items.map(x=><p key={x.id}>{x.productNameSnapshot} × {x.quantity} — {formatXaf(x.unitPriceXaf*x.quantity)}</p>)}{i&&<p><Link className="btn" href={`/factures/${i.id}`}>Voir la facture</Link></p>}</div></div></main>}
