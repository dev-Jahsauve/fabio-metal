import {NextResponse} from 'next/server'; import {z} from 'zod'; import {asc,eq} from 'drizzle-orm'; import {db} from '@/lib/db'; import {categories} from '@/lib/db/schema'; import {requireAdmin} from '@/lib/auth'; import {errorResponse} from '@/lib/api';
const schema=z.object({name:z.string().trim().min(2).max(100),slug:z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/).max(120)});
export async function GET(){try{await requireAdmin();return NextResponse.json(await db.select().from(categories).orderBy(asc(categories.name)))}catch(e){return errorResponse(e)}}
export async function POST(req:Request){try{await requireAdmin();const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Données invalides'},{status:400});const [row]=await db.insert(categories).values(p.data).returning();return NextResponse.json(row,{status:201})}catch(e){return errorResponse(e)}}
export async function PATCH(req:Request){try{await requireAdmin();const b=await req.json();const id=z.string().uuid().safeParse(b.id);const p=schema.partial().safeParse(b);if(!id.success||!p.success)return NextResponse.json({error:'Données invalides'},{status:400});const [row]=await db.update(categories).set(p.data).where(eq(categories.id,id.data)).returning();return NextResponse.json(row)}catch(e){return errorResponse(e)}}
export async function DELETE(req:Request){try{await requireAdmin();const b=await req.json();const id=z.string().uuid().safeParse(b.id);if(!id.success)return NextResponse.json({error:'Identifiant invalide'},{status:400});await db.delete(categories).where(eq(categories.id,id.data));return NextResponse.json({ok:true})}catch(e){return errorResponse(e)}}
