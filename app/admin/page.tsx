import { getCurrentUser } from "@/lib/auth"; import Link from "next/link"; import {db} from "@/lib/db"; import {products,orders,payments,contactRequests} from "@/lib/db/schema"; import {desc,eq} from "drizzle-orm"; import AdminConsole from '@/components/AdminConsole';
export default async function Admin(){const u=await getCurrentUser();if(!u||u.role!=="admin")return <main className="section"><div className="container"><div className="card"><h2>Administration protégée</h2><p>Connectez-vous avec un compte administrateur pour accéder à la gestion.</p><Link className="btn btn-gold" href="/connexion?next=/admin">Se connecter</Link></div></div></main>;const [rows,orderCount,paymentCount,contactCount]=await Promise.all([db.select().from(products).orderBy(desc(products.createdAt)),db.select({id:orders.id}).from(orders),db.select({id:payments.id}).from(payments).where(eq(payments.status,'success')),db.select({id:contactRequests.id}).from(contactRequests).where(eq(contactRequests.status,'new'))]);return <main className="section"><div className="container"><span className="eyebrow">Dashboard sécurisé</span><h1>Bonjour {u.name}.</h1><div className="stats"><div className="card"><strong>{rows.length}</strong><span>Produits</span></div><div className="card"><strong>{orderCount.length}</strong><span>Commandes</span></div><div className="card"><strong>{paymentCount.length}</strong><span>Paiements confirmés</span></div><div className="card"><strong>{contactCount.length}</strong><span>Demandes à traiter</span></div></div><AdminConsole initialProducts={rows}/><div className="cards" style={{marginTop:25}}><div className="card"><h3>Galerie</h3><p>Les APIs d’administration sont prêtes pour les vraies photos de l’atelier.</p></div><div className="card"><h3>Paiements</h3><p>CinetPay est intégré côté serveur, avec vérification et webhook HMAC. Les clés marchandes restent à configurer.</p></div><div className="card"><h3>Livraison</h3><p>Adresses et expéditions sont persistées. Le transporteur et le suivi restent configurables.</p></div><div className="card"><h3>Factures</h3><p>Une facture est créée uniquement après confirmation du paiement.</p></div></div></div></main>}
