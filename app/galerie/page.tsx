import { db } from "@/lib/db"; import { gallery } from "@/lib/db/schema"; import { eq, desc } from "drizzle-orm";
export default async function Galerie(){let gs:any[]=[];try{gs=await db.select().from(gallery).where(eq(gallery.published,true)).orderBy(desc(gallery.createdAt));}catch{}const refs=["https://upload.wikimedia.org/wikipedia/commons/3/3c/Iron_Gate_welding.jpg","https://upload.wikimedia.org/wikipedia/commons/c/cc/Soudeur_fabrication_d%27une_porte_fer.jpg"];return <main className="section"><div className="container"><div className="head"><span className="eyebrow">Galerie</span><h1>Réalisations & <span className="gradient">inspirations.</span></h1><p>Le dashboard permettra de publier les photos réelles de FABIOLE METAL et de les classer par catégorie.</p></div><div className="gallery">{(gs.length?gs.map(x=>x.imageUrl):refs).map((u,i)=><img key={i} src={u} alt="Fabrication métallique" />)}</div></div></main>}