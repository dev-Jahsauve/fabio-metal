import Link from "next/link";
import ServiceCarousel from "@/components/ServiceCarousel";
import AddToCartButton from "@/components/AddToCartButton";
import { db } from "@/lib/db";
import { products, services, gallery } from "@/lib/db/schema";
import { eq, asc, desc } from "drizzle-orm";
import { formatXaf, waLink } from "@/lib/utils";

const fallbackServices=[["POR","Portails & clôtures","Portails battants ou coulissants, grilles et ouvrages sur mesure."],["POR","Portes métalliques","Portes de sécurité et portes décoratives fabriquées selon vos dimensions."],["FEN","Fenêtres & grilles","Fenêtres métalliques, protections et ouvrages adaptés à votre habitation."],["MOB","Salle à manger","Tables, chaises et mobilier métallique réalisés à la demande."],["RDM","Porte-rideaux","Rideaux métalliques et solutions robustes pour commerces et locaux."],["SUR","Fabrication sur mesure","Vous avez un modèle ou un croquis ? L’atelier peut l’adapter et le fabriquer."]];
export default async function Home(){let ps:any[]=[],gs:any[]=[],sv:any[]=[];try{[ps,gs,sv]=await Promise.all([db.select().from(products).where(eq(products.published,true)).orderBy(desc(products.createdAt)).limit(6),db.select().from(gallery).where(eq(gallery.published,true)).orderBy(desc(gallery.createdAt)).limit(4),db.select().from(services).where(eq(services.published,true)).orderBy(asc(services.sortOrder)).limit(8)])}catch{}const serviceItems=sv.length?sv.map(s=>[s.icon||'MET',s.title,s.description||'Fabrication métallique sur mesure.']):fallbackServices;return <main><section className="container hero"><div><span className="eyebrow">Métallerie · Soudure · Fabrication sur mesure</span><h1>Le métal façonné pour <span className="gradient">vos projets.</span></h1><p>FABIOLE METAL conçoit et fabrique des ouvrages en fer pour particuliers, commerces et entreprises : portails, portes, fenêtres, mobilier, porte-rideaux et réalisations personnalisées.</p><div className="actions"><Link href="/boutique" className="btn btn-gold">Voir la boutique</Link><a className="btn" href={waLink("Bonjour FABIOLE METAL, je souhaite demander un devis pour un ouvrage métallique.")}>Contacter WhatsApp</a></div><p style={{fontSize:'.8rem',marginTop:18}}>Face à la mairie de Bojongo · +237 678 02 71 16</p></div><div className="hero-img"><div className="hero-caption"><strong>Du croquis à l’ouvrage.</strong><span>Une fabrication pensée selon vos dimensions, votre usage et votre style.</span></div></div></section><section className="section"><div className="container"><div className="head"><span className="eyebrow">Nos services</span><h2>Une offre claire, du <span className="gradient">sur-mesure</span> à la finition.</h2><p>Portails, portes, fenêtres, grilles, mobilier métallique et fabrication personnalisée.</p></div><ServiceCarousel items={serviceItems}/></div></section><section className="section" style={{background:'#0b0c0c'}}><div className="container"><div className="head"><span className="eyebrow">Boutique</span><h2>Des ouvrages disponibles ou <span className="gradient">personnalisables.</span></h2><p>Les prix sont calculés depuis la base et peuvent être administrés sans modifier le code.</p></div>{ps.length?<div className="shop-grid">{ps.map(p=><article className="product" key={p.id}>{p.imageUrl&&<img src={p.imageUrl} alt={p.name}/>}<div className="product-body"><h3>{p.name}</h3><p style={{color:'var(--muted)'}}>{p.description}</p><div className="price">{formatXaf(p.promoPriceXaf??p.priceXaf)} {p.promoPriceXaf&&<del className="old-price">{formatXaf(p.priceXaf)}</del>}</div><div className="actions"><Link className="btn" href={`/boutique/${p.slug}`}>Détails</Link><AddToCartButton productId={p.id} stock={p.stock}/></div></div></article>)}</div>:<div className="cards"><article className="card"><div className="icon">01</div><h3>Catalogue administrable</h3><p>Les articles et prix sont ajoutés depuis le dashboard.</p></article><article className="card"><div className="icon">02</div><h3>Panier sécurisé</h3><p>Le serveur recalcule les prix au moment de la commande.</p></article><article className="card"><div className="icon">03</div><h3>Sur mesure</h3><p>Les ouvrages personnalisés restent pilotables par devis.</p></article></div>}</div></section><section className="section"><div className="container"><div className="head"><span className="eyebrow">Galerie</span><h2>Quelques inspirations de <span className="gradient">métallerie.</span></h2><p>Les images de démonstration restent clairement des références et devront être remplacées par les photos réelles de FABIOLE METAL.</p></div><div className="gallery">{(gs.length?gs.map(g=>g.imageUrl):['https://upload.wikimedia.org/wikipedia/commons/3/3c/Iron_Gate_welding.jpg','https://upload.wikimedia.org/wikipedia/commons/c/cc/Soudeur_fabrication_d%27une_porte_fer.jpg']).map((u,i)=><img key={i} src={u} alt="Référence de fabrication métallique" />)}</div></div></section><section className="section"><div className="container about"><div><span className="eyebrow">FABIOLE METAL</span><h2>Une présence locale, une fabrication <span className="gradient">personnalisée.</span></h2><p style={{color:'var(--muted)'}}>Situé face à la mairie de Bojongo, l’atelier peut présenter ses créations, recevoir les demandes et publier progressivement son catalogue en ligne.</p></div><div className="card"><h3>Votre projet mérite un ouvrage adapté.</h3><p>Dimensions, modèle, finition et quantité sont précisés avant validation. Le prix des articles catalogue reste configurable par l’administrateur.</p><div style={{marginTop:18}}><a className="btn btn-gold" href={waLink("Bonjour FABIOLE METAL, je souhaite discuter d’un projet de fabrication métallique.")}>Parler du projet</a></div></div></div></section></main>}
