"use client";
import { useState } from "react";
export default function ForgotPassword(){const [email,setEmail]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setErr('');setMsg('');try{const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const j=await r.json();if(!r.ok){setErr(j.error||'Demande impossible');return}setMsg(j.message);if(j.devResetUrl)setMsg(`${j.message} Lien de test local : ${j.devResetUrl}`)}finally{setBusy(false)}}return <main className="section"><div className="container" style={{maxWidth:560}}><div className="card"><span className="eyebrow">Sécurité</span><h1>Mot de passe oublié</h1><form className="form" onSubmit={submit}><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label>{err&&<p className="error">{err}</p>}{msg&&<p>{msg}</p>}<button className="btn btn-gold" disabled={busy}>{busy?'Envoi...':'Recevoir le lien'}</button></form></div></div></main>}
