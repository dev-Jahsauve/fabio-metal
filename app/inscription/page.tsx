 "use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Inscription(){const [name,setName]=useState(""),[email,setEmail]=useState(""),[phone,setPhone]=useState(""),[password,setPassword]=useState(""),[err,setErr]=useState("");const r=useRouter();async function go(e:any){e.preventDefault();const x=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,email,phone,password})});const j=await x.json();if(!x.ok){setErr(j.error||"Inscription impossible");return}r.push("/compte");r.refresh()}return <main className="section"><div className="container" style={{maxWidth:560}}><div className="card"><span className="eyebrow">Créer un compte</span><h1>Votre espace client</h1><form className="form" onSubmit={go}><label>Nom<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Téléphone<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+237..."/></label><label>Mot de passe<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{err&&<p style={{color:"#ff8c8c"}}>{err}</p>}<button className="btn btn-gold">Créer mon compte</button></form></div></div></main>}