"use client";
import { useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
export default function ResetPassword(){const params=useSearchParams();const token=params.get('token')||'';const r=useRouter();const [password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm){setErr('Les mots de passe ne correspondent pas.');return}setBusy(true);setErr('');const res=await fetch('/api/auth/reset-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password})});const j=await res.json();setBusy(false);if(!res.ok){setErr(j.error||'Réinitialisation impossible');return}setMsg('Mot de passe modifié.');setTimeout(()=>r.push('/connexion'),900)}return <main className="section"><div className="container" style={{maxWidth:560}}><div className="card"><span className="eyebrow">Sécurité</span><h1>Nouveau mot de passe</h1><form className="form" onSubmit={submit}><label>Nouveau mot de passe<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirmer<input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{err&&<p className="error">{err}</p>}{msg&&<p>{msg}</p>}<button className="btn btn-gold" disabled={busy||!token}>{busy?'Modification...':'Modifier le mot de passe'}</button></form></div></div></main>}
