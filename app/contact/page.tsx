"use client";
import {useState} from 'react'; import {waLink} from '@/lib/utils';
export default function Contact(){const [f,setF]=useState({name:'',phone:'',subject:'Portail',message:''});const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const j=await r.json();setBusy(false);setMsg(r.ok?'Demande envoyée. FABIOLE METAL pourra vous recontacter.':j.error||'Erreur');}return <main className="section"><div className="container contact"><div><span className="eyebrow">Contact</span><h1>Parlons de votre <span className="gradient">ouvrage.</span></h1><p style={{color:"var(--muted)"}}>Face à la mairie de Bojongo<br/>+237 678 02 71 16</p><a className="btn btn-gold" href={waLink("Bonjour FABIOLE METAL, je souhaite demander un devis.")}>WhatsApp</a></div><div className="card"><form className="form" onSubmit={submit}><label>Nom<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})} placeholder="Votre nom"/></label><label>Téléphone<input required value={f.phone} onChange={e=>setF({...f,phone:e.target.value})} placeholder="+237..."/></label><label>Type d’ouvrage<select value={f.subject} onChange={e=>setF({...f,subject:e.target.value})}><option>Portail</option><option>Porte</option><option>Fenêtre / grille</option><option>Salle à manger</option><option>Porte-rideaux</option><option>Autre</option></select></label><label>Message<textarea required value={f.message} onChange={e=>setF({...f,message:e.target.value})} placeholder="Dimensions, modèle, quantité, finition..."/></label>{msg&&<p className={msg.startsWith('Demande')?'success':'error'}>{msg}</p>}<button className="btn btn-gold" disabled={busy}>{busy?'Envoi...':'Envoyer la demande'}</button></form></div></div></main>}
